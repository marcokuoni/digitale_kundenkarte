<!-- <script lang="ts">
  import { MAIL_TO, NOOPENER_NPREFERRER, PROCESS_ENV, TARGETS } from '../lib/const'
</script> -->

<p>
  Diese Funktion läuft nur wenn dem Profil eine validierte E-Mailadresse
  hinterlegt ist. 
  <!-- Ansonsten wende dich an <a href={`${MAIL_TO}${PROCESS_ENV.ADMIN_EMAIL_ADDRESS}`} target={TARGETS.BLANK} rel={NOOPENER_NPREFERRER}>{PROCESS_ENV.ADMIN_EMAIL_ADDRESS}</a> -->
</p>
